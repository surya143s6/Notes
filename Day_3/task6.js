var PI = 3.2;

var radius = prompt("Enter the number:");
// expression can be used in interpolation
var area = pi * radius ** 2; //** means exponential operator/

console.log(`The area for the given ${radius} cm is radius ${area}cm`);

//another way
